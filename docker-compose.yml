services:
  backend:
    build:
      context: ./backend
    environment:
      PORT: 3000
    expose:
      - "3000"

  frontend:
    build:
      context: ./frontend
    depends_on:
      - backend
    ports:
      - "8080:80"
